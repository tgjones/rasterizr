<fluent:RibbonWindow x:Class="Rasterizr.Demo.Views.MainWindowView"
					 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
					 xmlns:fluent="clr-namespace:Fluent;assembly=Fluent" 
					 xmlns:c="clr-namespace:Rasterizr.Demo.Controls"
                     Title="Rasterizr Studio" Width="800" Height="600">
	<DockPanel LastChildFill="True">
		<fluent:Ribbon DockPanel.Dock="Top">
			<!--Backstage Items-->
			<fluent:Ribbon.Menu>
				<fluent:Backstage>
					<fluent:BackstageTabControl>
						<fluent:Button Header="Open" Command="{Binding OpenCommand}" />
						<fluent:Button Header="Exit" Command="{Binding ExitCommand}" />
					</fluent:BackstageTabControl>
				</fluent:Backstage>
			</fluent:Ribbon.Menu>
		</fluent:Ribbon>
		<c:WriteableBitmapImage x:Name="ImageViewport" WriteableBitmap="{Binding OutputBitmap, Mode=OneWayToSource}" />
	</DockPanel>
</fluent:RibbonWindow>
