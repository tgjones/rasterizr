<UserControl x:Class="Rasterizr.SampleBrowser.TechDemos.Resources.TextureSampling.TextureSamplingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="200" />
		</Grid.ColumnDefinitions>
		<Image Grid.Column="0" Source="{Binding Bitmap}" Stretch="None" />
		<StackPanel Grid.Column="1" Margin="10">
			<Label>Texture filter</Label>
			<ComboBox ItemsSource="{Binding TextureFilters}" SelectedItem="{Binding SelectedTextureFilter}" />
		</StackPanel>
    </Grid>
</UserControl>
